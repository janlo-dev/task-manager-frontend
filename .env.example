# Copia este archivo a .env (o .env.local) y ajusta la URL si tu backend no está en local.
# URL base de la API REST de task-manager-api, incluyendo el prefijo /api.
VITE_API_URL=http://localhost:8080/api
